export async function searchVideos(query: string, apiKey: string): Promise<string[]> {
    console.log(`Searching Pexels for: ${query}`);
    // Placeholder: return empty or mock url
    return [];
}
